#SELECT * FROM first_flask.friends;

#INSERT INTO first_flask.friends(first_name, last_name, occupation)
#VALUES("Amamni", "Mkamba", "Husband"), ("Sheena", "Wright", "Wife")

#SELECT * FROM first_flask.friends;

#UPDATE first_flask.friends SET first_name = "Amani", last_name = "Mkamba", occupation = "Husband"
#WHERE id = 1

SELECT * FROM first_flask.friends;